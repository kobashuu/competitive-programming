/* インクルードなど */
#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    int N,M,count;
    cin >> N >> M;
    string a;
    string b;
    vector<string> c(M);
    for (int i = 0; i < M; i++) {
        count = 0
        cin << a[i] << b[i];
        // 友達同士を見つけてまとめる
        if （ find(c[count].begin(), c[count].end(), b[i]) ) {
            [i]
        } else {
            count += 1
            
        }
        c[s] += a[]
    }

}